services:
  # =========================================
  # 🌐 EliteHosting Web App
  # =========================================
  - type: web
    name: elitehosting-web
    runtime: node
    plan: free
    branch: main

    buildCommand: |
      pnpm install --frozen-lockfile
      pnpm build

    startCommand: |
      pnpm start

    autoDeploy: true

    
